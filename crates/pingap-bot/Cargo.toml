[package]
name = "pingap-bot"
version.workspace = true
edition = "2024"
license = "Apache-2.0"
keywords = ["pingap", "bot", "ja4h"]
description = "Bot management and client fingerprinting for pingap"
# Not published: this crate exists only inside this workspace.
publish = false

[lib]
name = "pingap_bot"
path = "src/lib.rs"

[dependencies]

[lints]
workspace = true
