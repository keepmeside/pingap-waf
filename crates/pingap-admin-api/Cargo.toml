[package]
name = "pingap-admin-api"
version.workspace = true
edition = "2024"
license = "Apache-2.0"
keywords = ["pingap", "admin", "api"]
description = "Admin API routes for pingap"
# Not published: this crate exists only inside this workspace.
publish = false

[lib]
name = "pingap_admin_api"
path = "src/lib.rs"

[dependencies]

[lints]
workspace = true
